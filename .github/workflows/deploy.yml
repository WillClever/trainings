name: Deploy Presentation

on:
  push:
    branches:
      - v2

jobs:
  deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version:
          - 18
          - 20

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}

    - name: Install Dependencies
      run: npm install

    - name: Build Presentation
      run: npm run build --if-present
    
    - name: test
      run: npm test  

    - name: Deploy to GitHub Pages
      run: |
        git config --global user.email "WilliamQuintero@Clever.com"
        git config --global user.name "William Quintero"
        echo "machine github.com login your-username password ${{ secrets.GITHUB_TOKEN }}" > ~/.netrc
        git push origin main
      env:
       GITHUB_TOKEN: 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
# prueba de deploy PRUEBA, no despliega